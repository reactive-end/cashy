/**
 * Tipos publicos del atomo GoogleIcon.
 * Renderiza el isotipo oficial de Google con sus cuatro colores corporativos.
 */

/** Propiedades del atomo GoogleIcon */
export interface GoogleIconProps {
  /** Lado del icono en pixeles (por defecto 20) */
  size?: number
}
